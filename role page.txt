import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TouchableOpacity,
} from "react-native";

export default function RoleSelectScreen() {
  const [role, setRole] = useState("user");

  return (
    <View style={styles.container}>
      {/* Heading */}
      <Text style={styles.heading}>Join as User Or Owner</Text>

      {/* User Card */}
      <TouchableOpacity
        style={[
          styles.card,
          role === "user" && styles.activeCard,
        ]}
        onPress={() => setRole("user")}
      >
        <View>
          <Text style={styles.cardText}>
            I'm a Student, looking for a
          </Text>
          <Text style={styles.cardHighlight}>
            PG’s / Hostels
          </Text>
        </View>

        <View
          style={[
            styles.radio,
            role === "user" && styles.radioSelected,
          ]}
        />
      </TouchableOpacity>

      {/* Owner Card */}
      <TouchableOpacity
        style={[
          styles.card,
          role === "owner" && styles.activeCard,
        ]}
        onPress={() => setRole("owner")}
      >
        <View>
          <Text style={styles.cardText}>
            I'm a Owner, hosting PG’s /
          </Text>
          <Text style={styles.cardHighlight}>
            Hostels
          </Text>
        </View>

        <View
          style={[
            styles.radio,
            role === "owner" && styles.radioSelected,
          ]}
        />
      </TouchableOpacity>
<Text style={roleStyles.buttonText}>Next</Text>
      </TouchableOpacity>
    </View>
  );
}


      
const PURPLE = "#6c47ff";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    padding: 20,
    justifyContent: "center",
  },

  heading: {
    fontSize: 22,
    fontWeight: "bold",
    marginBottom: 30,
  },

  card: {
    borderWidth: 1,
    borderColor: "#ddd",
    borderRadius: 12,
    padding: 16,
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    marginBottom: 16,
  },

  activeCard: {
    borderColor: PURPLE,
    backgroundColor: "#f4f2ff",
  },

  cardText: {
    fontSize: 14,
    color: "#333",
  },

  cardHighlight: {
    fontSize: 14,
    color: PURPLE,
    fontWeight: "bold",
    marginTop: 4,
  },

  radio: {
    width: 18,
    height: 18,
    borderWidth: 2,
    borderColor: PURPLE,
    borderRadius: 9,
  },

  radioSelected: {
    backgroundColor: PURPLE,
  },

  button: {
    backgroundColor: PURPLE,
    padding: 14,
    borderRadius: 10,
    alignItems: "center",
    marginTop: 20,
  },

  buttonText: {
    color: "#fff",
    fontWeight: "bold",
    fontSize: 16,
  },

  footer: {
    textAlign: "center",
    marginTop: 15,
    color: "#555",
  },

  login: {
    color: PURPLE,
    fontWeight: "bold",
  },
});
